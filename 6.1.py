# 6.1 一个简单的字典
# 字典（dict）是由一系列无序排列的可修改元素组成的集合
# Python中，字典用{}标记；用,分隔元素；元素为键值对，形式为键:值；键通常为不可变元素（字符串、数字、元组等）；
#     值可以为任意类型；不同键值对的键和值类型可以不同；键和值类型可以不同；不同键值对的键必须不同
alien_0 = {"color": "green", "points": 5}
print(alien_0["color"])
print(alien_0["points"])
